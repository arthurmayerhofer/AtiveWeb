<nav class="navbar navbar-expand-lg navbar-dark">
    <div class="container-fluid">
        <!-- Marca do site -->
        <div class="logo">
            <img src="awlogo.png" alt="Logo da Web Ative" class="logo">
        </div>
        <!-- Botão para alternar o menu em telas pequenas -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" tabindex="0"
            @onclick="ToggleNavMenu">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!-- Menu de navegação -->
        <div class="collapse navbar-collapse @(collapseNavMenu ? "" : "show")" id="navbarSupportedContent">
            <!-- Adicione os seus itens de menu personalizados aqui -->
            <ul class="navbar-nav ms-auto" style="text-align: right;"> <!-- Alinhamento dos itens à direita -->
                <!-- Links do menu com estilos personalizados -->
                <li class="nav-item">
                    <NavLink class="nav-link custom-link" href="" Match="NavLinkMatch.All"> <!-- Classe custom-link para estilos personalizados -->
                        Sites
                    </NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link custom-link" href="counter">
                        Marketing
                    </NavLink>
                </li>
                <li class="nav-item">
                    <NavLink class="nav-link custom-link" href="weather">
                        Design
                    </NavLink>
                </li>
                <!-- Adicione mais itens de menu conforme necessário -->
            </ul>
        </div>
    </div>
</nav>


<!-- JavaScript para alterar o background da barra de navegação ao rolar a página -->
@* <script>
    window.addEventListener('scroll', function () {
        var navbar = document.querySelector('.navbar');
        var scrollPosition = window.scrollY;

        // Calcula a intensidade da mudança de cor com base na posição de rolagem
        var intensity = scrollPosition / (0.3 * window.innerHeight); // Ajuste o valor 0.5 conforme necessário

        // Limita a intensidade entre 0 e 1
        intensity = Math.min(1, Math.max(0, intensity));

        // Calcula a opacidade com base na intensidade
        var opacity = intensity * 0.7; // Ajuste o valor 0.6 conforme necessário

        // Calcula a cor de fundo com base na opacidade
        var backgroundColor = 'rgba(0, 0, 0, ' + opacity + ')';

        // Aplica a cor de fundo à barra de navegação
        navbar.style.background = backgroundColor;
    });
</script> *@
<script>
    // Adicione um evento blur ao botão para remover o foco automaticamente
    document.querySelector('.navbar-toggler').addEventListener('click', function() {
        this.blur();
    });
</script>


@code {
    private bool collapseNavMenu = true;

    // Função para alternar a visibilidade do menu
    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }
}
